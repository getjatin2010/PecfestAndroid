<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/colorAccent" />
    <padding
        android:left="7dp"
        android:top="7dp"
        android:right="7dp"
        android:bottom="7dp"
        />
    <stroke
        android:width="15dp"
        android:dashWidth="@dimen/abc_dialog_padding_top_material"
        android:color="@color/colorAccent" />
    <corners
        android:radius="5dp"/>
</shape>   